name: workflow.yml

on:
  workflow_dispatch:

jobs:
  echo:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Echo repository name
      run: |
           echo "Hello, this is the repository: ${{ github.repository }}"